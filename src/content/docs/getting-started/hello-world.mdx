---
title: Hello World!
---

import Code from "../../../components/Code.astro";
import { FileTree } from "@astrojs/starlight/components";

import CMakeLists from "./CMakeLists.txt?raw";
import Main from "./main.cpp?raw";

> This page documents a simple Hello-World project

## CMake Setup

<FileTree>

- src
  - main.cpp
- **CMakeLists.txt**

</FileTree>

It is recommended to consume saucer through CMake (see [Installation](/getting-started/)).  
While it is recommended to use _CPM.cmake_, the following example will use [FetchContent](https://cmake.org/cmake/help/latest/module/FetchContent.html) for simplicty.

<Code lang="cmake" code={CMakeLists} title="CMakeLists.txt" />

## Code Setup

<FileTree>

- src
  - **main.cpp**
- CMakeLists.txt

</FileTree>

Now that CMake is setup, it is time to write a simple saucer program!

<Code lang="cpp" code={Main} title="src/main.cpp" mark={[
    { range: "9-10",label: "1. Change the Window Title..." },
    { range: "12-17",label: "2. Expose a native function that sums two numbers" },
    { range: "19-25",label: "3. Expose a native function that returns a random number" },
    { range: "22-23",label: "4. Evaluate JavaScript Code and capture the result" },
    { range: "29-30",label: "5. Don't forget to show the window!" },
    { range: "32-33",label: "6. This will resolve when the last window is closed" },
]} showLineNumbers />
